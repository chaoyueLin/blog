# Android客户端网络&安全（P7面试完整版Markdown）
> 移除【架构&稳定性】章节，完整补充OkHttp专题，基于思维导图全部节点补充原理、实战坑点、面试问答，可直接复制保存为md文件

## 一、基础核心概念
### 1. 幂等
- **定义**：同一个请求多次发起，业务执行结果保持一致，不会产生副作用
- **目的**：防止重复支付、重复扣款、重复创建订单；网络抖动重试导致重复提交
- **实现方案**
  1. **业务唯一请求ID**：客户端生成bizRequestId，每次业务请求唯一，服务端做幂等表
  2. **防抖**：客户端短时间屏蔽重复点击
  3. **状态机校验**：服务端校验订单状态，已处理订单拒绝二次执行
  4. **数据库唯一索引**：利用数据库约束拦截重复数据
- **实战坑**：支付接口**禁止自动重试**，重试会直接破坏幂等逻辑

### 2. 熔断、降级、限流
|类型|作用|触发条件|客户端实现|
|---|---|---|---|
|限流|控制请求频率，避免短时间大量请求打垮服务端|短时间高频调用|OkHttp拦截器，令牌桶/漏桶算法|
|熔断|服务端大量报错，直接切断请求，快速失败|连续N次接口失败，打开熔断窗口|统计接口错误率，熔断时间窗口内直接返回本地兜底|
|降级|牺牲非核心业务，保障核心链路可用|服务压力大/故障|关闭推荐、弹窗等非核心，返回缓存兜底数据|

> 核心思想：**快失败、保核心，不要无限等待堆积请求**

### 3. HTTPS & 证书绑定(Certificate Pinning)
- HTTPS原生作用：传输数据加密，防止网络链路窃听篡改
- **原生HTTPS缺陷**：只校验证书是否被系统信任CA签发，**不校验是不是我方服务器证书**
- 中间人攻击场景：用户手动安装Charles/Fiddler根证书；Root设备写入系统根证书；CA机构密钥泄露
- **证书绑定原理**：APP内置服务器证书公钥指纹，TLS握手时，除系统证书链校验外，额外比对公钥指纹，指纹不匹配直接拒绝连接
- **坑点**：只绑定单证书，证书轮换更新，老版本APP全部断网；**必须预埋备用证书指纹，配置远程降级开关**
- 代码：OkHttp `CertificatePinner` / res/xml 网络安全配置

### 4. Token / JWT认证
- Token：服务端下发会话凭证，每次请求Header携带
- JWT：自包含token，payload存储简单业务信息，服务端无需查库即可解析
- 安全存储：Android Keystore加密存储，禁止明文存SP；内存用完置空；不要打印日志输出token
- 常见逻辑：401鉴权失败，统一触发token刷新，刷新失败跳转登录

### 5. 弱网优化
- 策略：请求优先级、动态超时、重试策略、本地兜底缓存
- 优先级：支付、登录等高优先级优先调度；埋点统计可延迟执行
- 超时：区分普通接口/大文件上传下载动态设置超时时间，不要全部写死
- 重试：指数退避重试，**幂等接口才允许重试，支付绝对禁止重试**
- 兜底：弱网下读取本地缓存，展示旧数据，提升用户体验

## 二、网络 & 接口 & 协作
### 1. 接口规范
- 统一返回体 `{code:int, msg:string, data:any}`
  - code：业务状态码，0成功，非0业务错误
  - msg：提示文案，用于toast展示
  - data：业务返回数据
- 配套：接口版本version、分页page/pageSize、通用签名字段

### 2. HTTP错误码处理策略
- 401：token失效，重新登录
- 403：权限拒绝
- 404：接口不存在，版本/地址错误
- 5xx：服务端故障，本地缓存兜底，有限次数重试
- 网络异常：无网络、超时、证书异常区分提示，不要统一提示“网络错误”

### 3. 支付接口特殊约束
- ✅必须做幂等
- ✅禁止自动重试
- ✅请求参数完整日志上报（脱敏）
- ✅增加状态机校验，客户端不要以本地判断支付成功，以服务端回调为准

### 4. 问题定位流程
`现象复现 → 抓包(Charles) → Postman复现服务端行为 → 查看客户端完整日志 → 提取证据，前后端联调`
> 区分：客户端参数问题？网关拦截？服务端业务异常？

### 5. 签名 & 防重放攻击
- 参数：业务参数 + timestamp时间戳 + nonce随机字符串
- 流程：全部参数字典排序拼接，加盐做md5/SHA256生成sign签名放入请求
- 防重放：服务端校验timestamp时间窗口（比如5分钟），校验nonce是否已经使用过，拒绝过期/重复请求

### 6. 统一网络架构
`UI层 → ViewModel → 业务Repository → 数据源(Remote远程网络 / Local本地缓存)`
- Repository层聚合网络与本地缓存，UI不直接依赖OkHttp/Retrofit
- 职责分离：网络异常、签名、token统一在Repository/网络拦截器处理

## 📌补充专题：OkHttp 完整原理&面试点
### 1. OkHttp整体架构
- 核心角色：`OkHttpClient`（全局单例，连接池、分发器、配置）、`Request`、`Response`、`Call`、`RealCall`
- **Call**：代表一次请求；`execute()`同步；`enqueue()`异步
- 推荐：**全局单例OkHttpClient，不要每次new**；连接池、线程池复用，减少握手开销

### 2. 核心流程（面试默写）
1. 构建Request
2. 生成RealCall，交给`Dispatcher`分发器
3. Dispatcher：维护同步队列、异步就绪队列、异步运行队列，控制最大并发数
4. 执行`getResponseWithInterceptorChain()`，**拦截器链**
5. 一层层拦截器处理，拿到Response返回

#### 五大内置拦截器（顺序不能乱）
1. **RetryAndFollowUpInterceptor**：重试、重定向处理
2. **BridgeInterceptor**：补全请求头（Content‑Type、Keep‑Alive、gzip解压response）
3. **CacheInterceptor**：缓存逻辑（HTTP缓存）
4. **ConnectInterceptor**：建立连接，连接池复用Socket
5. **CallServerInterceptor**：真正socket读写，和服务器通信

> 自定义拦截器：`addInterceptor(应用拦截器)`、`addNetworkInterceptor(网络拦截器)`
- **应用拦截器**：最外层，不走缓存也会执行；不会收到重定向/重试的多次调用；适合打印日志、加公共参数
- **网络拦截器**：靠近socket，重定向、重试都会多次执行；可以看到原始网络报文，抓包调试

### 3. Dispatcher分发器
- 异步：最大并发64，同一个host最多5个请求
- 队列：ready就绪队列、running运行队列；运行超过阈值，放入就绪等待
- 异步不是开无限线程，内部`RealCall.AsyncCall`提交线程池执行

### 4. ConnectionPool连接池（Socket复用）
- HTTP1.1 keep‑alive，同一个host的socket可以复用多次请求，减少TCP三次握手+TLS握手耗时
- 默认：最大5个空闲连接，空闲连接存活5分钟自动回收
- 问题：大量短连接不使用单例OkHttpClient，会大量创建Socket，出现端口耗尽

### 5. HTTP缓存 CacheInterceptor
- 缓存介质：DiskLruCache磁盘缓存
- 遵循HTTP缓存头：`Cache‑Control、Expires、ETag、Last‑Modified`
- 强缓存：直接读本地，不发网络请求
- 协商缓存：发请求带If‑None‑Match，服务端返回304，复用本地缓存
> 注意：POST请求OkHttp**默认不缓存**

### 6. 重试机制 RetryAndFollowUpInterceptor
- 默认开启重试；**只会重试可恢复异常：连接失败、握手失败**
- ❗**不会重试4xx业务错误、5xx业务返回**；不是只要报错就重试
- 重定向最多跟随20次；
> 实战：支付接口，自定义拦截器关闭重试，防止重复提交

### 7. HTTPS证书绑定 CertificatePinner
```kotlin
val certificatePinner = CertificatePinner.Builder()
    .add("api.xxx.com", "sha256=xxxxxx")
    .build()
val client = OkHttpClient.Builder()
    .certificatePinner(certificatePinner)
    .build()
```
- 在ConnectInterceptor TLS握手阶段校验公钥指纹
- 坑：证书更新，老APP直接网络异常，预埋备用sha256指纹

### 8. 超时设置（4种超时）
```kotlin
OkHttpClient.Builder()
    .connectTimeout(10,TimeUnit.SECONDS)   //TCP连接建立
    .readTimeout(10,TimeUnit.SECONDS)      //socket读
    .writeTimeout(10,TimeUnit.SECONDS)     //socket写
    .callTimeout(30,TimeUnit.SECONDS)     //整个call完整总超时
```
- callTimeout：整个请求完整生命周期总超时，包含重试、重定向全部时间

### 9. 常见面试问题
1. OkHttpClient为什么建议单例？
> 持有连接池、线程池、缓存；频繁new会不断创建销毁socket，握手开销大，端口占用。

2. 应用拦截器和网络拦截器区别？
> 应用拦截器：最外层，重试重定向只执行一次；网络拦截器靠近socket，每次网络交互都会执行。

3. Dispatcher并发控制怎么做？
> 异步最大64并发，单host最多5；超过就放入就绪队列等待空闲。

4. HTTP缓存POST为什么不生效？
> OkHttp默认缓存只支持GET请求，POST不做磁盘缓存。

5. 怎么实现熔断限流？
> 自定义Interceptor，统计接口失败率，本地做令牌桶，拦截请求直接返回本地结果。

## 三、安全 & 风控 & 合规
1. **客户端安全对抗**
- 防Root：检测su二进制文件，检测root标识
- 防模拟器：设备特征、硬件信息检测
- 防调试：关闭debuggable，ptrace反调试
- 防抓包：证书绑定；检测代理环境
- 防截屏录屏：窗口设置FLAG_SECURE，敏感页面禁止截屏
> 注意：客户端安全只能增加破解成本，无法做到100%安全

2. **敏感数据存储**
- 用户密码、密钥、token等敏感信息，**禁止明文存SharedPreferences**
- 使用Android Keystore硬件加密存储，密钥不暴露到应用进程内存
- 使用完及时清空内存，不要长期持有敏感字符串
- 日志脱敏：禁止打印密码、身份证、token明文

3. **安全键盘**
- 金融输入密码场景，不使用系统EditText键盘
- 自定义安全键盘：按键乱序布局，防止键盘监听、内存dump窃取密码
- 密码不经过系统输入法进程，规避输入法窃取风险

4. **金融风控**
- 设备指纹：设备唯一标识、硬件信息
- 行为风控：点击频率、操作间隔、异常快速操作检测
- 风险请求：风控token随业务请求上报，服务端做风险判断

5. **防重复支付**
- 客户端：防抖、唯一业务请求ID
- 服务端：幂等校验、状态机校验、数据库唯一索引
> 不能只依赖客户端防护，客户端可以被篡改，服务端校验才是根本

6. **合规**
- 权限：按需申请权限，隐私协议合规
- 日志脱敏：敏感字段身份证、手机号、银行卡必须脱敏输出
- 用户隐私数据不上报、不本地裸存

7. **热修复限制**
> 资金链路、核心安全链路**严禁热修复**
- 热修复存在被篡改风险，安全类代码走完整版本发布流程；防止恶意补丁篡改支付逻辑

## 四、线上问题 & P7能力
### 线上故障排查
1. **支付大面积失败**
`止血(关闭功能/降级开关) → 收集日志抓包定位根因 → 修复发版 → 复盘，输出故障报告，建立预防机制`

2. **接口成功率下跌排查**
    1. 监控看错误码分布、版本分布、机型分布、地区分布
    2. 拉取受害用户完整日志，抓包报文
    3. 区分：客户端参数问题？网关拦截？服务端故障？运营商网络问题？
    4. 紧急止血，修复，复盘

### 质量保障手段
- 监控：线上各项指标实时告警
- 灰度发布：小流量验证，逐步放量
- 远程开关：故障可以关闭问题功能
- CodeReview：变更强制CR，拦截风险代码

### P7高级工程师核心能力
1. **架构设计**：能设计业务架构、网络架构，考虑高可用、降级、兼容
2. **复杂线上问题排查**：独立处理疑难线上故障，定位根因，推动修复
3. **跨团队协作**：推动后端、测试、产品协同解决跨端问题
4. **制定规范**：团队编码规范、接口规范、安全规范
5. **带人、扛故障**：指导初级同学，线上重大故障兜底，故障复盘沉淀

---

# 高频面试背诵题
## Q1：HTTPS已经加密，为什么还要证书绑定？
> HTTPS只能保证证书来自系统信任CA，不能保证证书属于我方服务器。
> 如果用户安装恶意根证书，中间人可以伪造合法证书，绕过普通HTTPS完成抓包篡改。
> 证书绑定额外校验服务端证书公钥指纹和APP内置指纹匹配，抵御中间人攻击；但是需要预埋备用证书指纹，配置降级开关，防止证书更新老版本APP全部断网。

## Q2：熔断、限流、降级三者区别
- **限流**：控制请求频率，避免短时间大量请求压垮服务端；
- **熔断**：接口大量报错，直接切断请求，快速失败，保护服务端；
- **降级**：关闭非核心业务，牺牲次要功能，保障核心业务可用。
客户端可以基于OkHttp拦截器实现，核心原则快失败，不堆积请求。

## Q3：OkHttp应用拦截器和网络拦截器区别
> 应用拦截器：位于最外层，重定向、重试只会执行一次，适合添加公共参数、打印业务日志；
> 网络拦截器靠近Socket，每次网络交互都会执行，可以拿到原始网络报文，适合调试抓包。

## Q4：OkHttp为什么推荐全局单例？
> OkHttpClient持有连接池ConnectionPool、线程池、缓存实例；频繁new会反复创建销毁Socket，TCP/TLS握手开销大，还会造成端口资源浪费。

## Q5：什么是防重放攻击，怎么做？
攻击者截获请求报文，重复多次发送相同请求。
请求携带timestamp时间戳 + nonce随机字符串，参与签名；服务端校验时间窗口，校验nonce是否重复使用，拒绝过期、重复请求。

## Q6：幂等怎么实现，支付接口注意点？
幂等：多次请求业务结果只生效一次。
方案：客户端唯一请求ID、服务端幂等表、状态机、数据库唯一索引。
> 支付接口：必须幂等，**禁止自动重试**，业务结果以服务端回调为准，不要相信客户端本地状态。

## Q7：客户端敏感数据如何安全存储？
使用Android Keystore加密存储，禁止明文存SP；内存使用完成及时清空；日志打印脱敏，不要输出明文敏感信息。客户端安全只能增加破解成本，不能完全依赖客户端，关键校验必须服务端做。

需要我把这份内容导出为可直接复制的纯净无markdown标题说明的纯md文本吗？

内容由 AI 生成