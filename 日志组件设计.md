
# android 自研日志组件｜从零完整设计思路 + 优秀开源框架借鉴取舍

## 一、先讲：从零自研「前置设计考量点」（核心思考）

我在设计日志组件时，**不是照着开源库抄**，而是先站在工程稳定性、性能、安全、线上问题角度，定义「必须解决的问题」：

### 1. 性能层面必须考虑

1. **绝对不能阻塞主线程**：文件 IO、磁盘写入必须异步，避免 ANR、卡顿
2. **高并发日志不能卡顿、不能丢日志、不能乱序**
3. **不能频繁打开关闭文件句柄**（IO 开销极大）
4. **需要缓冲区批量写盘**，减少系统调用
5. 日志量大时要**削峰**，不能瞬间大量 IO

### 2. 稳定性层面必须考虑

1. **不能因为日志组件崩溃导致 App 崩溃**（组件内部异常全部吞掉、兜底）
2. **磁盘空间保护**：磁盘满、低空间时自动停止写日志，不抛异常
3. **避免日志无限膨胀**：必须控制单文件大小、文件数量、过期清理
4. **App Crash / 被杀场景尽可能减少日志丢失**
5. **多进程并发日志不乱序、不冲突**

### 3. 安全层面必须考虑

1. **所有日志必须统一脱敏**（手机号、身份证、密码、token）
2. **线上禁止明文敏感日志输出**
3. 可关闭低级日志，不能靠删代码

### 4. 业务架构层面必须考虑

1. **统一门面入口**，业务零侵入
2. **日志分级管控**：Debug 全量、Release 只保留 Warn/Error
3. **多输出渠道可插拔**：Logcat、本地文件、上报、控制台可自由开关
4. **支持线上动态配置**（后台动态开启日志排查问题）
5. **日志可格式化、可溯源**（时间、线程、进程、堆栈）

### 5. 线上运维层面必须考虑

1. **日志不能实时上报**（耗流量、耗电）
2. 只在「崩溃重启、用户反馈、WiFi 空闲」触发上报
3. 日志可压缩、可采样
4. 可区分开发环境 / 测试环境 / 生产环境策略

---

## 二、基于上面的「自研需求」，再选择、借鉴优秀开源框架

### 我整体设计思路：

**架构借鉴 Timber、文件能力借鉴 XLog、调试体验借鉴 Logger、最终自研整合并补齐所有短板**

## 1. 架构设计：完全借鉴 Timber（最佳解耦模型）

### 借鉴 Timber 优点

1. **门面模式 + Tree 分发架构**
   - 业务只调用统一 API，底层完全解耦
   - 打印、输出、存储、上报完全分离
2. **可插拔输出渠道**
   - 可以自由开启 / 关闭 Logcat、文件日志、上报日志
3. **自动 Tag、简洁 API**，业务零成本接入
4. **分级过滤机制**，Release 快速屏蔽低级别日志

### 我弥补 Timber 的缺点（自研增强）

1. Timber**无本地持久化** → 我自研完善文件落盘
2. Timber**无多进程支持** → 我自研多进程隔离策略
3. Timber**无脱敏、无磁盘管控** → 全部自研补齐

---

## 2. 文件持久化、性能、稳定性：核心借鉴 XLog（工业级能力）

### 借鉴 XLog 核心设计思想

1. **文件切割策略**：按大小切割 + 按时间过期清理
2. **磁盘保护机制**：最大文件数、保留天数、磁盘上限
3. **异步批量刷盘、缓冲区设计**，减少 IO 次数
4. **多进程日志隔离思想**：不同进程独立日志文件，避免文件锁冲突
5. **高并发不丢日志、有序输出**

### 我规避 XLog 的缺点（自研优势）

1. XLog 是二进制加密日志，**无法直接查看** → 我自研**明文结构化日志**，便于排查
2. XLog 需要引入 so、包体积大 → 我纯 Kotlin 实现，无 NDK 依赖
3. XLog **无脱敏、无业务定制能力** → 我自研统一脱敏、动态配置、日志上报、采样策略

---

## 3. 调试体验：借鉴 Logger

### 借鉴优点

1. JSON 自动格式化
2. 堆栈、线程信息清晰
3. 日志排版美观，开发调试效率高

### 规避缺点

Logger **不适合线上**、性能差、无持久化 → 我只在 Debug 环境启用美化，Release 关闭

---

## 三、最终：我自研日志组件的「整合后完整设计方案」

### 1. 架构（Timber Tree 思想）

- 统一门面入口
- 分发器 + 多 Printer 适配器
- 可插拔：LogcatPrinter、FilePrinter、UploadPrinter

### 2. 性能（XLog IO 思想）

- 单线程异步串行写盘，保证有序、不卡顿
- 缓冲区批量刷盘、定时 flush
- 控制单文件大小、自动切割、自动清理

### 3. 稳定性（XLog 工程能力增强）

- 多进程隔离（pid 区分文件）
- 磁盘满兜底保护
- 组件异常全部内部捕获，不影响业务
- Crash 前尽可能落盘日志

### 4. 安全（自研独有增强）

- 全局统一脱敏处理器
- 禁止敏感信息明文输出
- Release 自动降级日志级别

### 5. 线上运维（自研独有）

- 动态后台配置日志级别
- 崩溃 / 反馈触发上传、压缩、采样
- 区分环境策略

---

## 四、面试一句话满分总结（直接背）

我设计日志组件时，**先从性能、稳定性、安全、线上运维四个维度定义工程必须解决的问题**，然后借鉴主流框架优点：
**架构解耦、可插拔分发参考 Timber；文件持久化、IO 性能、日志切割清理、多进程稳定性参考 XLog；开发调试体验参考 Logger**。
同时规避了三者缺点：补齐了脱敏、动态配置、线上可控、磁盘保护、无 so 包体积负担，最终实现一套轻量、稳定、可线上运维的自研日志框架。

---

## 五、面试高频追问答案（配套）

### Q：为什么不直接用 XLog，要自研？

XLog 工业级稳定但是太重、二进制日志不方便排查、无可插拔架构、缺少业务安全脱敏和动态配置能力。自研可以结合 Timber 架构优势 + XLog 稳定 IO 能力，同时补齐业务安全和线上运维能力，更适合业务长期迭代。

### Q：为什么不直接用 Timber？

Timber 架构优雅但只有打印能力，缺少持久化、文件管理、多进程、稳定性能力，线上无法落地。

### Q：你的自研日志比开源库强在哪？

1. 架构比 XLog 优雅、可扩展
2. 稳定性、IO 能力远超 Timber
3. 自带脱敏、动态配置、运维上报，所有开源库都没有整套业务闭环
4. 纯 Kotlin、无 so、包体积小