# Android 组件化架构 + ARouter 完整面试手册（大纲+细节）
## 一、整体大纲（面试必背框架）
### 1. 组件化 & 模块化核心区别
- **模块化**：按技术职责分层（网络、图片、工具、基础库），单向依赖、无业务拆分
- **组件化**：按业务拆分（登录、首页、订单、支付），**可独立运行、完全解耦、无直接依赖**

### 2. 组件化标准分层架构（从上至下）
1. 宿主壳工程（空壳，只打包初始化）
2. 业务组件层（login/home/order/pay）
3. 业务 API 接口层（各组件对外暴露接口）
4. 基础功能模块层（network/image/permission）
5. 通用底层公共层（utils/ext/db）

### 3. 组件化四大核心难题 & 解决方案
1. **组件解耦**：路由跳转 + 接口下沉 + 禁止直接依赖
2. **组件通信**：ARouter页面跳转 + IProvider跨组件服务 + FlowBus事件通知
3. **独立调试**：Gradle动态切换 Application / Library
4. **依赖闭环**：接口下沉、禁止业务组件互相依赖实现层

### 4. ARouter 核心原理大纲
1. **编译期 APT**：扫描 @Route，生成路由分组表、路径表、服务表
2. **运行时初始化**：加载分组懒加载、缓存路由信息到仓库
3. **运行时跳转**：路径解析 → 拦截器 → 实例化 → 跳转
4. **跨组件服务**：IProvider 接口下沉，实现跨组件方法调用

### 5. 组件化高频坑点大纲
- 依赖闭环、资源冲突、Manifest合并冲突、组件初始化冲突、ARouter找不到页面、独立运行报错、内存泄漏

---

# 二、详细知识点（面试完整版）
## 1. 模块化 VS 组件化（精准区分）
### 1.1 模块化（技术分层）
- 拆分依据：**技术能力、通用功能**
- 模块：base、network、image-loader、utils、permission
- 依赖规则：**单向树状依赖，下层被上层依赖**
- 特点：**不能单独运行、无业务隔离**

### 1.2 组件化（业务拆分）
- 拆分依据：**业务线、业务模块**
- 组件：login、home、mine、order、pay、setting
- 依赖规则：**业务组件之间 0 依赖**，只依赖公共API、基础库
- 特点：
  1. 开发态：是 Application，可独立编译、独立运行
  2. 集成态：是 Library，打包进入宿主 App
  3. 彻底解决大型项目耦合、编译慢、多人并行开发冲突

## 2. 组件化标准架构分层（企业规范）
### 2.1 五层架构（严格单向依赖）
1. **App宿主壳**
   - 无任何业务代码
   - 负责：全局Application、打包配置、版本管理、全局初始化调度

2. **业务组件层（核心业务）**
   所有业务独立：登录、首页、订单、支付

3. **组件API层（解耦核心）**
   每个业务组件对应一个 `-api` 模块
   - 只存放：对外接口、实体类、路由常量
   - **只定义、不实现**

4. **基础功能层**
   网络、图片、弹窗、权限、WebView、埋点

5. **底层通用层**
   工具类、扩展函数、加密、本地存储

### 2.2 强制依赖规范（杜绝闭环）
- ✅ 允许依赖：宿主 → 业务组件 → 组件API → 基础库
- ❌ 禁止：业务组件互相依赖、下层依赖上层、同层互相依赖

## 3. 组件化四大核心问题 & 解决方案
### 3.1 问题1：组件耦合（Activity直接跳转、类直接引用）
**解决方案：路由解耦（ARouter）**
- 所有页面跳转不用类名，用**字符串路径**
- 所有跨组件方法调用，依赖**API接口 + IProvider**

### 3.2 问题2：组件通信三种场景
1. **页面跳转、传参** → ARouter 路由
2. **跨组件同步方法调用（获取用户信息、发起支付）** → ARouter IProvider
3. **跨组件事件通知（登录成功/支付完成）** → FlowBus / LiveDataBus

### 3.3 问题3：组件独立调试（最重要工程能力）
#### 实现方案：Gradle 动态开关
根目录 `gradle.properties` 定义：
```
IS_RUN_LOGIN=false
IS_RUN_ORDER=false
```
组件 `build.gradle`：
```groovy
if (IS_RUN_LOGIN.toBoolean()) {
    apply plugin: 'com.android.application'
} else {
    apply plugin: 'com.android.library'
}

android {
    defaultConfig {
        if (IS_RUN_LOGIN.toBoolean()) {
            applicationId = "com.xxx.login"
        }
    }
}
```
#### 独立调试配套解决
- 单独运行时：自带调试Manifest、独立Application、模拟网络环境
- 集成打包时：自动变为Library，合并宿主Manifest

### 3.4 问题4：依赖闭环（循环依赖）
#### 产生原因
A依赖B、B又依赖A（业务实现类互相引用）

#### 终极解决方案：接口下沉
1. A、B各自抽出 **xxx-api** 模块
2. A 依赖 B-api，B 依赖 A-api
3. **双方只依赖接口，不依赖实现**
4. 实现类保留在自身业务组件中
👉 彻底消灭循环依赖

## 4. 组件化解耦细节（资源 / Manifest / 初始化）
### 4.1 资源解耦
- 所有资源加模块前缀：`login_btn / home_icon`
- 关闭依赖传递：`transitive = false`
- 公共颜色、尺寸统一下沉 Base

### 4.2 Manifest 解耦
- 业务组件独立Manifest
- 集成模式自动合并
- 四大组件全部通过 ARouter 注册，宿主不管理

### 4.3 组件初始化解耦
问题：所有组件初始化写在宿主Application，极其臃肿
解决方案：
- 使用 **Jetpack AppStartup / ARouter AutoRegister**
- 每个组件初始化逻辑**内聚在自身模块**
- 支持优先级、懒初始化、异步初始化

## 5. ARouter 完整原理（面试核心）
### 5.1 ARouter 三大核心能力
1. 页面路由跳转（解耦Activity）
2. IProvider 跨组件服务通信（解耦业务方法）
3. 全局拦截器、降级、参数自动注入

### 5.2 编译期原理（APT 核心）
使用 **注解处理器 APT**，编译阶段扫描所有 `@Route`

自动生成三类文件：
1. **ARouter$$Group$$xxx**：分组索引表（组名→分组类）
2. **ARouter$$Path$$xxx**：路径→Activity/Class映射表
3. **ARouter$$Providers$$xxx**：服务接口→实现类映射表

优势：
- **编译期生成，运行时0反射扫描**
- 启动快、性能高

### 5.3 运行时初始化原理
`ARouter.init()`
1. 扫描所有自动生成的 Group 类
2. 加载**分组索引**到内存仓库 Warehouse
3. **懒加载机制**：只加载分组，不加载全量路径表
4. 某个分组第一次跳转时，才加载该组所有Path详情

👉 解决：初始化耗时、冷启动卡顿

### 5.4 一次完整跳转流程
```
build(path) → 解析分组 → 懒加载组路由表
→ 执行拦截器链 → 匹配目标Class
→ 封装Intent参数 → 跳转
→ @Autowired 自动注入参数
```

### 5.5 IProvider 跨组件通信原理
1. API模块定义接口 `ILoginService : IProvider`
2. 业务组件实现接口，并添加 `@Route`
3. ARouter编译期记录服务映射表
4. 运行时通过 `navigation(ILoginService::class)` 获取实现类
5. 实现**无耦合跨组件方法调用**

### 5.6 拦截器原理
- 自定义 IInterceptor + priority优先级
- **子线程执行**（不能直接UI操作）
- 可实现：登录拦截、权限拦截、路由降级、埋点统计

## 6. ARouter 高频面试题（标准答案）
### 6.1 为什么 Path 必须两级 /group/page？
- ARouter **分组懒加载机制**需要拆分组名
- 一级路径无法分组，会导致全部路由表初始化加载，拖慢启动

### 6.2 ARouter 如何解决组件化依赖闭环？
1. 页面跳转用字符串，不依赖Activity类
2. 跨组件调用依赖**API抽象接口**，不依赖实现类
3. 所有业务组件互不依赖，彻底解耦

### 6.3 ARouter 懒加载原理
初始化只加载**Group分组表**
使用某个页面时，再加载该组全部Path表
极大减少App启动内存与耗时

### 6.4 @Autowired 原理
编译期生成辅助注入类
跳转完成后自动反射赋值，无需手动getIntent

### 6.5 ARouter 跳转失败常见原因
1. 未配置 kapt、未引入compiler
2. path书写错误、非两级路径
3. 混淆未配置，路由类被混淆删除
4. 多进程未判断主进程初始化
5. 组件独立运行未加载当前组件路由表

### 6.6 ARouter 与隐式跳转 Scheme 区别
- ARouter：编译期校验、支持拦截、自动注入、组件化专用
- Scheme：多用于外部唤起、无编译校验、容易出错

## 7. 组件化面试终极背诵模板（可直接口述）
1. **组件化是按业务拆分模块，实现业务完全解耦、独立编译运行；模块化是按技术分层，统一提供底层能力。**
2. **组件化通过四层解耦：路由解耦页面、API接口解耦服务、事件总线解耦通知、Gradle开关支持独立调试。**
3. **通过接口下沉彻底解决依赖闭环，所有业务组件只依赖对方API，不依赖实现。**
4. **ARouter核心是APT编译期生成路由表 + 分组懒加载，实现无耦合页面跳转与跨组件服务调用，是组件化架构的核心基石。**

---

# 三、整体知识闭环（面试官最终提问）
### 面试官：你项目组件化整体架构怎么设计的？
**满分回答：**
我项目采用标准五层组件化架构：宿主壳、业务组件、组件API层、基础功能层、通用底层。
所有业务组件完全解耦，不存在互相依赖。
页面跳转全部使用ARouter路由，通过APT编译期生成路由表、分组懒加载保证性能；
跨组件方法调用采用IProvider+API接口下沉，彻底解决依赖闭环；
跨组件事件通信使用FlowBus；
通过Gradle动态Application/Library开关实现组件独立调试；
组件初始化采用AppStartup按需初始化，避免宿主臃肿。
整体实现了**高解耦、可独立编译、低耦合、易维护**的企业级组件化架构。
这份文档**先大纲、后细节、带面试满分话术**，完全适配高级Android面试。需要我帮你**提炼一页纸极简背诵版**，方便你考前快速复盘吗？

内容由 AI 生成